import styled from 'styled-components'

const Wrapper = styled.div`
    display: grid;
    grid-template-columns: 24px 1fr 48px;
    width: 100%;
    align-items: center;
`
export const Styled = {
    Wrapper,
}
